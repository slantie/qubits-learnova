import { CoursesDashboard } from '@/components/backoffice/courses/CoursesDashboard';

export const metadata = {
    title: 'Courses | Backoffice',
};

export default function CoursesPage() {
    return <CoursesDashboard />;
}
